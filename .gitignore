# --- 1. GENERAL OPERATING SYSTEM/IDE FILES ---
.DS_Store
Thumbs.db
.vscode
.idea

# --- 2. OLD PROJECT OBSOLETE FILES (Aggressive Ignore) ---
# Ignore the empty folders left over from the old project structure
/vendor
/node_modules
/public

# Ignore obsolete .env file if it's still floating around
.env
/public/build 
/public/storage

# --- 3. LARAVEL BACKEND PROJECT IGNORES (CRITICAL) ---

# Ignore the vendor folder (dependencies) inside the backend folder
/backend/vendor

# Ignore the node_modules folder (front-end tools) inside the backend folder
/backend/node_modules

# Ignore sensitive credentials and cache specific to the Laravel app
/backend/.env
/backend/.env.testing
/backend/storage/*.key
/backend/storage/*.crt
/backend/storage/logs/*.log
/backend/bootstrap/cache

# Ignore symbolic link that holds uploaded files
/backend/public/storage
/backend/public/build 
/backend/public/hot